@using ShoppingCart.Models.Models.User
@using ShoppingCart.BackOffice.Infrastructure
@model IEnumerable<ApplicationRole>
@{ 
    ViewBag.Title = "Roles"; 
}

<h2>Roles</h2>
<p style="margin-bottom:10px;">
    @Html.ActionLink("Create", "Create", null, new { @class = "btn btn-primary" })
</p>
    <table class="table">
        <tr><th>ID</th><th>Name</th><th>Users</th><th></th></tr>
        @if (Model.Count() == 0)
        {
            <tr><td colspan="4" class="text-center">No Roles</td></tr>
        }
        else {
            foreach (ApplicationRole role in Model)
            {
                <tr>
                    <td>@role.Id</td>
                    <td>@role.Name</td>
                    <td>
                        @if (role.Users == null || role.Users.Count == 0)
                        {
                            @: No Users in Role
}
                        else {
                            <p>
                                @string.Join(", ", role.Users.Select(x => Html.GetUserName(x.UserId)))
                        </p>
                        }
                    </td>
                    <td>
                        @using (Html.BeginForm("Delete", "Role", new { id = role.Id }))
                        {
                            @Html.ActionLink("Edit", "Edit", "Role", new { id = role.Id }, new { @class = "btn btn-primary btn-xs" })
                            <button class="btn btn-danger btn-xs"
                                    type="submit">
                                Delete
                            </button>
                        }
                    </td>
                </tr>
            }
        }
    </table>

