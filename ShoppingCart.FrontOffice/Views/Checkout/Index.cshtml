
@model ShoppingCart.ViewModels.CartDTO
@{
    ViewBag.Title = "Checkout summary";
}

<style>
    #checkTable td {
        vertical-align: middle;
    }
</style>

<h2>Checkout Summary</h2>

    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h4>Checkout Details: Please confirm your order</h4>
        <hr />
        <div class="form-group">
            <div class="control-label col-md-2">Ship to:</div>
            <div class="col-md-10">
                @Html.DisplayFor(x => x.UserName)
            </div>
        </div>


        <table id="checkTable" class="table">
            <thead>
                <tr>
                    <th>Quantity</th>
                    <th>Item</th>
                    <th class="text-right">Price</th>
                    <th class="text-right">Subtotal</th>
                </tr>
            </thead>
            <tbody>
               
                @foreach (var line in Model.Cart.CartLines)
                {
                    <tr>
                        <td class="text-center">@line.Quantity</td>
                        <td class="text-left">@line.Product.Name</td>
                        <td class="text-right">@line.Product.Price.ToString("c")</td>
                        <td class="text-right">
                            @((line.Quantity * line.Product.Price).ToString("c"))
                        </td>
                    </tr>

                }
            </tbody>
            <tfoot>
                <tr>
                    <td colspan="3" class="text-right">Total:</td>
                    <td class="text-right">
                        
                        @*@Model.Cart.ComputeTotalValue().ToString("c")*@
                    </td>
                </tr>
            </tfoot>
        </table>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10 text-center">
                @Html.ActionLink("Return to Cart", "Index", "Carts",
                     new { returnUrl = "/" },
                     new { @class = "btn btn-primary" }
                )
                @*<input class="btn btn-primary"
                       type="submit" value="Confirm order" />*@
                @Html.ActionLink("Complete Order", "Order", "Checkout", Model, new { @Class = "btn btn-primary" })
            </div>
        </div>
    </div>
}

